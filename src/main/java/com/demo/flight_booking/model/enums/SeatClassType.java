package com.demo.flight_booking.model.enums;

/**
 * Represents different seat classes available on an aircraft.
 * It is used to devide seats into classes and determine the fee.
 */
public enum SeatClassType {
    ECONOMY,
    PREMIUM,
    BUSINESS,
    FIRST_CLASS
}
